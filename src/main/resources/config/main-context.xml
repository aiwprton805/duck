<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:/config/database/database.properties" />
    <context:component-scan base-package="com.magistr.duck.service.impl" />

    <!-- ========================== DATA SOURCE ========================== -->
    <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource"
        destroy-method="close">
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!-- ============================== DAO ============================== -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="configLocation" value="classpath:/config/database/mybatis/config.xml" />
    </bean>

    <bean id="dao" class="org.mybatis.spring.support.SqlSessionDaoSupport" abstract="true">
        <property name="sqlSessionFactory" ref="sqlSessionFactory" />
    </bean>

    <bean id="userDao" class="com.magistr.duck.dao.mybatis.UserMybatisDao" parent="dao" />
    <bean id="termDao" class="com.magistr.duck.dao.mybatis.TermMybatisDao" parent="dao" />
    <bean id="termInfoDao" class="com.magistr.duck.dao.mybatis.TermInfoMybatisDao" parent="dao" />

    <!-- ============================ SERVICE ============================ -->
    

</beans>
